info:
  name: Create connector
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/connectors
  body:
    type: json
    data: |-
      {
        "connectorType": "TEE",
        "diameterMm": 50,
        "material": "COPPER",
        "latitude": 48.7395,
        "longitude": 19.1531
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
