info:
  name: Get customer list
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/v1/customers?page=10&size=10
  params:
    - name: page
      value: "10"
      type: query
    - name: size
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
